{
  "name": "web-app-business-center",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prepare": "husky",
    "type-check": "yarn --cwd frontend type-check && yarn --cwd backend type-check",
    "lint": "yarn --cwd frontend lint && yarn --cwd backend lint",
    "knip": "yarn --cwd frontend knip && yarn --cwd backend knip",
    "format": "yarn --cwd frontend format && yarn --cwd backend format",
    "verify": "yarn type-check && yarn lint && yarn knip"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
